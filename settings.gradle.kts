pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "FlickrElectrolux"
include(":FlickrElectroluxAndroidApp")
include(":shared")